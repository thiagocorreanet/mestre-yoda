# Security policy

Report vulnerabilities privately to the repository maintainers. Do not include
credentials, customer code, private repository contents, or production data in
a public issue.

Mestre Yoda refuses symbolic links during project inspection, skips dependency
and build directories, limits the number and size of files it reads, and never
executes project code or package managers. The `0.1.x` provider is read-only.
